"""Simple script corrected to valid Python.

Assumptions:
- The original file used `jac` fences; this repository appears to be Python-oriented, so the code is converted to Python.
"""

def lovejac() -> str:
    return "with jac python becomes even more awesome!"


if __name__ == "__main__":
    print(lovejac())